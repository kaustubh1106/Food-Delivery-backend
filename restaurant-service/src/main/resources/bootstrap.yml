server:
  port: 8200
spring:
  application:
    name: restaurant-service
  cloud:
    consul:
      host: ${CONSUL_HOST:localhost}
      port: 8500
      config:
        enabled: true
        format: YAML
        prefixes:
        - config
        data-key: data

