server:
  port: 8600
spring:
  application:
    name: review-service
  cloud:
    consul:
      host: ${CONSUL_HOST:localhost}
      port: 8500
      config:
        enabled: true
        format: YAML
        prefixes:
          - config
        data-key: data
