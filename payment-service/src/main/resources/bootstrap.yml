server:
  port: 8550
spring:
  application:
    name: payment-service
  cloud:
    consul:
      host: ${CONSUL_HOST:localhost}
      port: 8500
      config:
        enabled: true
        format: YAML
        prefixes:
          - config
        data-key: data
