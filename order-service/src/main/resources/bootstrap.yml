server:
  port: 8400
spring:
  application:
    name: order-service
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        format: YAML
        prefixes:
        - config
        data-key: data

